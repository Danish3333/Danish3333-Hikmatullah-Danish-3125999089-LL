# Root requirements — covers all experiments and homeworks snd assignments
# Install with: pip install -r requirements.txt

# Experiment 1 — Rainfall Alert (Streamlit dashboard)
requests==2.32.3
streamlit==1.35.0
pandas==2.2.2
pydeck==0.9.1
python-dotenv==1.0.1

# Experiment 2, 3, 4 — Scientific computing
numpy>=1.26.0
scipy>=1.13.0
matplotlib>=3.8.0

# Testing (all experiments)
pytest==8.2.2

# Homework 2 — CNN digit recognition
torch>=2.3.0
torchvision>=0.18.0
